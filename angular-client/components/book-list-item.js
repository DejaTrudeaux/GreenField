angular.module('app')
  .controller('BookListItemCtrl', () => {
  })
  .component('bookListItem', {
    bindings: {
    },
    controller: 'BookListItemCtrl',
    templateUrl: '/templates/book-list-item.html',
  });
